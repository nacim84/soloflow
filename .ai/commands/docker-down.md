# Docker Compose Down

Stop all Docker containers for the project

## Actions:
1. Execute `docker-compose down` to stop and remove containers
2. Display status confirmation

## What this does:
- Stops PostgreSQL database
- Stops PGAdmin
- Removes containers (volumes are preserved)
- Cleans up networks

Use `/docker-up` to start containers again.
